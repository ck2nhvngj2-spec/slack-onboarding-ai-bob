# Seed Project Architecture
**Version**: 1.0.0
**Core Stack**: Node.js, Express, TypeScript.

## Guidelines
- All authentication logic is centralized in `auth.ts`.
- The payments module is owned by the Billing Team (Lead: Sarah Jenkins).
- Every new route must be documented and include unit tests.